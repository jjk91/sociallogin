/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import TopBar from './src/components/TopBar.contaniner';

AppRegistry.registerComponent(appName, () => TopBar);
